# Django Settings
DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

# Database
DB_NAME=ai_agent_db
DB_USER=ai_agent_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# JWT Settings
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ACCESS_TOKEN_LIFETIME=60  # minutes
JWT_REFRESH_TOKEN_LIFETIME=1440  # minutes (24 hours)

# File Storage Settings
# FILE_STORAGE_ROOT=/path/to/custom/storage  # Optional: custom storage path
# FILE_STORAGE_MAX_SIZE=1073741824  # Optional: 1GB max size

# Redis (for Celery)
REDIS_URL=redis://localhost:6379/0

# Email Settings (optional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password

# CORS Settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# n8n Webhook Settings
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/magic-link
N8N_RESET_PASSWORD_WEBHOOK_URL=https://your-n8n-instance.com/webhook/farmon-reset-password

# Frontend URL for magic links
FRONTEND_URL=http://localhost:3000

# NTFY Notification Settings
NTFY_SERVER_URL=https://ntfy.hvacvoice.com
NTFY_DEFAULT_TOPIC=farmon
NTFY_DEFAULT_EMAIL=